export

class Person {
    firstName;
    eyecolor;
    length;
    birthyear;

    constructor(firstName, eyecolor, length, birthyear) {
      this.firstName = firstName;
      this.eyecolor = eyecolor;
      this.length = length;
      this.birthyear = birthyear;
    }
  }